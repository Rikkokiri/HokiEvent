<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.virginiatech.piraj.hokievent">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".LoginActivity"
            android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateEventActivity"
            android:label="@string/createEventTitle"/>

        <activity android:name=".CreateAccountActivity"
            android:label="@string/createAccountTitle"/>

        <activity android:name=".EventDetailsActivity"
            android:label="@string/eventDetailsTitle"/>

        <activity android:name=".ProfileActivity"
            android:label="@string/profileViewTitle"/>

        <activity android:name=".EditProfileActivity"
            android:label="@string/editProfileTitle"/>

        <activity android:name=".InterestsActivity"
            android:label="@string/interestsActivityTitle"/>

        <activity android:name=".SearchActivity"
            android:label="@string/searchActivityTitle"/>

        <activity android:name=".SearchResultsActivity"
            android:label="@string/searchResultsTitle"/>

        <activity android:name=".HomeActivity"
            android:label="@string/homeActivityTitle"></activity>

        <!-- Google Maps API key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAtiIEOjmrbN5-3DdpXXYOOdVks1oR71mQ"/>
    </application>

</manifest>